{
  "scripts": {
    "dev": "npm --prefix swift_delivery_frontend run dev --",
    "build": "npm --prefix swift_delivery_frontend run build",
    "lint": "npm --prefix swift_delivery_frontend run lint",
    "preview": "npm --prefix swift_delivery_frontend run preview"
  }
}
